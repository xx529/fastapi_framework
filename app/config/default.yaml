appserver:
  name: myapp
  version: 0.0.1
  host: localhost
  port: 8003
  workers: 5
  debug: true

pg:
  host: localhost
  port: 5432
  user: postgres
  db_schema: myapp
  password: "123456"
  database: postgres

redis:
    host: localhost
    port: 6379
    db: 0
    password: ""
    max_connections: 10
    project_prefix: "myapp"
    timeout: 2
    expire_seconds: 60

kafka:
  host: localhost
  port: 9092
  enable: true
  topics:
    chat_task:
      topic_name: dev_1_topic
      num_consumers: 1
    log_task:
      topic_name: dev_2_topic
      num_consumers: 1

external_api:
  myapp:
    host: localhost
    port: 8003
    prefix: ''
    protocol: http

log:
  level: DEBUG